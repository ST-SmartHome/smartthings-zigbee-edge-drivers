name: safety-visonic-gb540-core
components:
  - id: main
    capabilities:
      - {id: accelerationSensor, version: 1}
      - {id: tamperAlert, version: 1}
      - {id: batteryLevel, version: 1}
    categories:
      - {name: MultiFunctionalSensor}
