name: safety-motion-swann-core
components:
  - id: main
    capabilities:
      - {id: motionSensor, version: 1}
      - {id: tamperAlert, version: 1}
      - {id: batteryLevel, version: 1}
      - {id: refresh, version: 1}
    categories:
      - {name: MotionSensor}
