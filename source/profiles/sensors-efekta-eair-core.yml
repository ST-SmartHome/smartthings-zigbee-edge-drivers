name: sensors-efekta-eair-core
components:
  - id: main
    capabilities:
      - {id: carbonDioxideMeasurement, version: 1}
      - {id: battery, version: 1}
      - {id: batteryLevel, version: 1}
      - {id: refresh, version: 1}
    categories:
      - {name: AirQualityDetector}
