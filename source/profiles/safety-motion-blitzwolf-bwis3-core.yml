name: safety-motion-blitzwolf-bwis3-core
components:
  - id: main
    capabilities:
      - {id: motionSensor, version: 1}
      - {id: refresh, version: 1}
    categories:
      - {name: MotionSensor}
