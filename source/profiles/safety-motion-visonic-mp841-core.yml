name: safety-motion-visonic-mp841-core
components:
  - id: main
    capabilities:
      - {id: motionSensor, version: 1}
      - {id: refresh, version: 1}
    categories:
      - {name: MotionSensor}
