name: sensors-ikea-vindstyrka-core
components:
  - id: main
    capabilities:
      - {id: temperatureMeasurement, version: 1}
      - {id: relativeHumidityMeasurement, version: 1}
      - {id: fineDustSensor, version: 1}
      - {id: refresh, version: 1}
    categories:
      - {name: AirQualityDetector}
